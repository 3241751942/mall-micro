<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zzl.productservice.mapper.ProductMapper">

    <!-- 结果映射：将查询结果映射到 ProductResponseDTO -->
    <resultMap id="ProductResponseDTOMap" type="com.zzl.productservice.entity.ProductResponseDTO">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="description" property="description"/>
        <result column="price" property="price"/>
        <result column="status" property="status"/>
        <result column="images" property="images"/>
        <result column="sales" property="sales"/>
        <result column="create_time" property="createTime"/>
        <result column="category_name" property="categoryName"/>
        <result column="brand_name" property="brandName"/>
    </resultMap>

    <!-- 商品详情查询 -->
    <select id="selectProductDetail" resultMap="ProductResponseDTOMap">
        SELECT
            p.id,
            p.name,
            p.description,
            p.price,
            p.status,
            p.images,
            p.sales,
            p.create_time,
            c.name AS category_name,
            b.name AS brand_name
        FROM product p
                 LEFT JOIN category c ON p.category_id = c.id AND c.deleted = 0
                 LEFT JOIN brand b ON p.brand_id = b.id AND b.deleted = 0
        WHERE p.id = #{productId} AND p.deleted = 0
    </select>

    <!-- 分页商品查询-->
    <select id="selectProductPage" resultMap="ProductResponseDTOMap">
        SELECT
        p.id,
        p.name,
        p.description,
        p.price,
        p.status,
        p.images,
        p.sales,
        p.create_time,
        c.name AS category_name,
        b.name AS brand_name
        FROM product p
        LEFT JOIN category c ON p.category_id = c.id AND c.deleted = 0
        LEFT JOIN brand b ON p.brand_id = b.id AND b.deleted = 0
        <where>
            p.deleted = 0
            <if test="categoryId != null">
                AND p.category_id = #{categoryId}
            </if>
            <if test="brandId != null">
                AND p.brand_id = #{brandId}
            </if>
            <if test="minPrice != null">
                AND p.price >= #{minPrice}
            </if>
            <if test="maxPrice != null">
                AND p.price &lt;= #{maxPrice}
            </if>
            <if test="keyword != null and keyword != ''">
                AND p.name LIKE CONCAT('%', #{keyword}, '%')
            </if>
        </where>
        <if test="sortBy != null and sortBy != ''">
            ORDER BY
            <choose>
                <when test="sortBy == 'price'">p.price</when>
                <when test="sortBy == 'sales'">p.sales</when>
                <when test="sortBy == 'createTime'">p.create_time</when>
                <otherwise>p.id</otherwise>
            </choose>
            <if test="order != null and order == 'asc'">ASC</if>
            <if test="order == null or order != 'asc'">DESC</if>
        </if>
        <if test="sortBy == null or sortBy == ''">
            ORDER BY p.create_time DESC
        </if>
    </select>


</mapper>