server:
  port: 8993

spring:
  application:
    name: auth-service
  cloud:
    nacos:
      discovery:
        username: nacos
        password: nacos
        server-addr: 127.0.0.1:8848
        namespace: public
      config:
        enabled: false
        server-addr: 127.0.0.1:8848
        file-extension: yaml
        namespace: public
        username: nacos
        password: nacos
jwt:
  secret: yourBase64EncodedSecretKeyAtLeast32BytesLong   # 请使用 base64 编码的 32 位以上密钥
  expiration: 86400000